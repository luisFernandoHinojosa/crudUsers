export const ConfirmDeleteUser = () => {
    return (
        <section
            className={`fixed top-0 bottom-0 left-0 right-0  border-2 border-green-300 bg-red-700/40 flex justify-center items-center  ${
                showFilteredUser ? 'visible opacity-100' : 'invisible opacity-0'
            }`}
            
        >
            <div>

            </div>
            
        </section>
    );
};
